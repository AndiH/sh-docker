# Smart Home Docker [Compose] Stack

A collection of Docker Compose files which make up parts of my smart home (plus more).

The data directory is usually skipped and can found seperately (eventually).
